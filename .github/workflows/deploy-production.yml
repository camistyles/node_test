name: Deploy production env

on: 
  push:
    branches:
      - "master"
  workflow_dispatch:
    
jobs:
  ci-other-file:
    environment: test
    uses: camilo-rojas/node_test/.github/workflows/build-node.yml@master
    with:
      codeCoverage: ${{ secrets.CI_COVERAGE_MIN }}
