name: ci run

on:
  workflow_call:
    inputs:
      codeCoverage:
        required: true
        type: string
jobs:
  test_and_coverage:
    runs-on: ubuntu-latest
    container:
      image: camistyles/test-node-ci:node-test
    steps:
      - name: "test_coverage"
        run: "test.sh"

